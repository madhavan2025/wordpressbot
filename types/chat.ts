export interface ChatSettings {
  name: string;
  chatIconSize: number;
  chatIcon: string;
  headerBg: string;
  headerTextColor: string;
  windowBg: string;
  borderColor: string;
  borderRadius: string;
  shadow: string;
}
